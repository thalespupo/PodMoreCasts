<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.tapura.podmorecasts">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/SplashTheme"
        android:name=".MyApplication">
        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity
            android:name=".discover.DiscoverPodcastActivity"
            android:label="@string/title_activity_discover_podcast"
            android:theme="@style/AppTheme.NoActionBar" />
        <activity
            android:name=".details.PodcastDetailsActivity"
            android:theme="@style/AppTheme.NoActionBar" />
        <activity
            android:name=".player.PlayerTestActivity"
            android:label="@string/title_activity_player_test"
            android:theme="@style/SplashTheme"></activity>
    </application>

</manifest>